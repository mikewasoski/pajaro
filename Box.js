class Box extends BaseClass {
  constructor(x, y, width, height) {
    super(x,y,width,height)
    thisimage =loadImage("sprites/box.png")
         };
   
       };
       display()
         var pos = this.body.position;
         pos.x = mouseX;
         pos.y = mouseY;
         var angle = this.body.angle;
        super.display();